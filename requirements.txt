# Dependencies for local dev / IDE go-to-definition (ManifestDeclarativeSource, etc.)
# Runtime uses Docker base image airbyte/source-declarative-manifest:6.54.6.
# Install: python -m venv .venv && .venv/bin/pip install -r requirements.txt

airbyte-cdk>=3.9.0
PyYAML>=6.0
